# Backend proxy config
PORT=3001

# Base URL for readiness, route, allAxisTypes, changeValidRoadTypes, validAxisTypes, axisType, blockage
ROUTING_BASE_URL=https://routing-web-service-ityenzhnyq-an.a.run.app

# Path override (keep defaults unless your server differs)
PATH_READY=/ready
PATH_VALID_AXIS_TYPES=/validAxisTypes
PATH_AXIS_TYPE=/axisType
PATH_ROUTE=/route
PATH_BLOCKAGE=/blockage
PATH_ALL_AXIS_TYPES=/allAxisTypes
PATH_CHANGE_VALID_ROAD_TYPES=/changeValidRoadTypes

# CORS for local dev
CORS_ORIGIN=http://localhost:5173
